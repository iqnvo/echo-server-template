# 🎨 Echo Server Template

```
  ______      _          
 |  ____|    | |         
 | |__   ____| |__   ___  
 |  __| / __| '_ \ / _ \ 
 | |___| (__| | | | (_) |
 |______\___|_| |_|\___/ 
                         
   Server Template
```
<img width="1873" height="555" alt="image" src="https://github.com/user-attachments/assets/0ba07a5a-d519-4256-952a-3298dec34bc6" />


---

## 📖 القصة

> ### **مشروع تعليمي من عدة سنوات مضت**
>
> صنعته اثناء تعلمي لأساسيات بايثون
> صنعت هذا القالب **لأغراض الدراسة فقط** - كان الهدف تعلم كيفية بناء واجهة جميلة لتطبيقات الـ Terminal.  
> **لا يحتوي على أي وظائف شبكية** (تم حذفها لأغراض قانونية).  
> هذا **قالب جاهز** يمكن دمجه مع أي أداة تحتاج واجهة احترافية.

---

## 💡 الفكرة

الكثير من المطورين يصنعون أدوات قوية لكن **يهملون الشكل والواجهة**.  
هذا المشروع يقدم:
- ✨ واجهة Terminal جميلة وملونة
- 🎨 بانر احترافي مع ألوان متدرجة
- ⚙️ نظام إدارة إعدادات بسيط
- 📦 هيكل مشروع منظم وقابل للتوسع

---

## ⚠️ تنبيه مهم

**هذا قالب واجهة فقط - ليس سيرفر حقيقي!**
- تم إزالة كل الوظائف الشبكية
- صُنع للأغراض التعليمية فقط
- يمكن استخدامه كقالب لمشاريعك

---

## 🎯 الاستخدام المقترح

يمكنك دمج هذا القالب مع:
- 🛠️ أدوات سطر الأوامر
- 🔧 سكربتات Python
- 📊 تطبيقات تحليل البيانات
- 🤖 برامج الأتمتة
- أي مشروع يحتاج واجهة Terminal احترافية

---

## 📂 هيكل المشروع

```
Cn/
│
├── Server.py                 # الملف الرئيسي - نقطة البداية
│
├── Configure/                # إعدادات المشروع
│   └── init_server.py       # فحص وتثبيت المكتبات تلقائياً
│
├── data/                     # إدارة البيانات
│   ├── data_manager.py      # وظائف قراءة وكتابة الإعدادات
│   └── server.json          # ملف الإعدادات (Port, Address, Listen)
│
└── venom/                    # مجلد للعميل (Client)
    └── Client.py            # ملف فارغ - جاهز للتطوير
```

---

## 🚀 التشغيل

### المتطلبات

- Python 3.x
- المكتبات (يتم تثبيتها تلقائياً):
  - `pystyle` - للألوان والتأثيرات
  - `pyfiglet` - للبانر ASCII
  - `colorama` - لدعم الألوان
  - `PyQt5` - للواجهات (اختياري)

### التشغيل

```bash
# 1. استنساخ المشروع
git clone https://github.com/iqnvo/echo-server-template.git
cd echo-server-template/Cn

# 2. تشغيل السيرفر
python Server.py
```

**عند التشغيل لأول مرة:**
- البرنامج سيفحص المكتبات المطلوبة
- سيثبت المكتبات الناقصة تلقائياً
- قد يطلب إعادة التشغيل بعد التثبيت

---

## 💻 الواجهة

### البانر الرئيسي

```
     ______      _          
    |  ____|    | |         
    | |__   ____| |__   ___  
    |  __| / __| '_ \ / _ \ 
    | |___| (__| | | | (_) |
    |______\___|_| |_|\___/ 
                            
      Server Template

    This Tool Created By '! La La la'    Discord -> @uuuc
```

### الأوامر المتاحة

| الأمر | الوصف |
|------|------|
| `1` | تشغيل السيرفر العادي (عرض الإعدادات فقط) |
| `cls` / `clear` | مسح الشاشة وإعادة عرض البانر |

---

## ⚙️ ملف الإعدادات (server.json)

```json
{
   "address": "auto",
   "port": 6969,
   "listen": "?"
}
```

**الحقول:**
- `address`: عنوان السيرفر (افتراضي: auto)
- `port`: منفذ الاتصال (افتراضي: 6969)
- `listen`: عدد الاتصالات (افتراضي: ?)

يمكنك تعديل هذه القيم حسب احتياجك.

---

## 🔧 شرح الملفات

### 1️⃣ Server.py (الملف الرئيسي)

**الوظائف الرئيسية:**

```python
class server:
    def __init__(self):
        self.configure()           # تهيئة البرنامج
        self.command_terminal()    # استقبال الأوامر
    
    def banner(self):
        # عرض البانر الملون
    
    def command_terminal(self):
        # معالجة أوامر المستخدم
    
    def normal_server(self):
        # عرض معلومات السيرفر
```

**المميزات:**
- ✅ كشف نظام التشغيل تلقائياً (Linux/Windows)
- ✅ إخفاء المؤشر للحصول على واجهة نظيفة
- ✅ ألوان متدرجة جميلة
- ✅ بانر مركّز تلقائياً حسب حجم Terminal

---

### 2️⃣ Configure/init_server.py

**الوظيفة:**
- فحص المكتبات المطلوبة
- تثبيت المكتبات الناقصة تلقائياً
- إعلام المستخدم بالحاجة لإعادة التشغيل

**المكتبات المفحوصة:**
```python
libraries = [
    "pystyle",      # للألوان والتأثيرات
    "pyfiglet",     # للبانر ASCII
    "requests",     # طلبات HTTP
    "numpy",        # معالجة البيانات
    "pandas",       # تحليل البيانات
    "tabulate",     # عرض الجداول
    "colorama"      # دعم الألوان
]
```

---

### 3️⃣ data/data_manager.py

**الوظائف المتاحة:**

| الوظيفة | الوصف |
|---------|-------|
| `write_info_about_server()` | إنشاء ملف server.json إذا لم يكن موجود |
| `get_port()` | قراءة رقم المنفذ |
| `get_address()` | قراءة العنوان |
| `get_listen()` | قراءة عدد الاتصالات |

**مثال الاستخدام:**
```python
import data.data_manager as data_manager

port = data_manager.get_port()        # 6969
address = data_manager.get_address()  # "auto"
listen = data_manager.get_listen()    # "?"
```

---

### 4️⃣ venom/Client.py

ملف فارغ جاهز للتطوير.  
يمكنك إضافة:
- كود العميل (Client)
- وظائف الاتصال
- أي ميزات إضافية

---

## 🎨 التخصيص

### تغيير البانر

في `Server.py`:
```python
class server:
    titleBanner:str = "اسمك\nالمشروع"  # غير هذا
```

### تغيير الألوان

```python
# الألوان المتاحة في pystyle
Colors.blue_to_purple    # أزرق إلى بنفسجي
Colors.blue_to_white     # أزرق إلى أبيض
Colors.red_to_yellow     # أحمر إلى أصفر
Colors.green_to_cyan     # أخضر إلى سماوي
```

### إضافة أوامر جديدة

في دالة `command_terminal()`:
```python
match r:
    case "1":
        self.normal_server()
    
    case "2":  # أمر جديد
        self.your_new_function()
    
    case "help":
        self.show_help()
```

---

## 📊 أمثلة تطوير

### مثال 1: إضافة قائمة مساعدة

```python
def show_help(self):
    _print("=" * 50, color=Colors.white)
    _print("Available Commands:", color=Colors.blue_to_purple)
    _print("1 - Start Server", color=Colors.white)
    _print("2 - Your Command", color=Colors.white)
    _print("help - Show This Menu", color=Colors.white)
    _print("=" * 50, color=Colors.white)
```

### مثال 2: إضافة إحصائيات

```python
def show_stats(self):
    from datetime import datetime
    
    _print("=" * 50, color=Colors.white)
    _print(f"Server Time: {datetime.now()}", color=Colors.blue_to_purple)
    _print(f"Port: {data_manager.get_port()}", color=Colors.white)
    _print(f"Status: Running", color=Colors.green_to_cyan)
    _print("=" * 50, color=Colors.white)
```

---

## 🛠️ نصائح للتطوير

### ✅ أفضل الممارسات

1. **احتفظ بالهيكل المنظم**
   - ضع كل وظيفة في الملف المناسب
   - استخدم مجلد `Configure` للإعدادات
   - استخدم مجلد `data` للبيانات

2. **استخدم الوظائف الجاهزة**
   - `_print()` للطباعة الملونة
   - `_input()` للإدخال الملون
   - `data_manager` لإدارة البيانات

3. **اختبر على أنظمة مختلفة**
   - الكود يدعم Windows و Linux
   - تأكد من التوافق

---

## 🔄 أفكار للتطوير

- [ ] إضافة نظام تسجيل الدخول
- [ ] حفظ سجل الأحداث (Logging)
- [ ] واجهة PyQt5 رسومية
- [ ] نظام Plugins قابل للتوسع
- [ ] قاعدة بيانات SQLite
- [ ] واجهة Web بسيطة

---

## 📝 ملاحظات

### لماذا `venom/Client.py` فارغ؟

تم إزالة كود العميل لأنه كان يحتوي على وظائف شبكية.  
يمكنك بناء عميلك الخاص حسب احتياجك.

### لماذا تم إزالة وظائف الشبكة؟

**لأغراض قانونية وأخلاقية.**  
هذا قالب تعليمي - ليس للاستخدام في أنشطة غير قانونية.

---

## 🤝 المساهمة

هل لديك فكرة لتحسين القالب؟

1. Fork المشروع
2. أنشئ فرع (`git checkout -b feature/improvement`)
3. Commit التغييرات (`git commit -m 'Add improvement'`)
4. Push للفرع (`git push origin feature/improvement`)
5. افتح Pull Request

---

## 📄 الترخيص

هذا المشروع مفتوح المصدر - استخدمه كما تشاء.  
**تذكير:** للأغراض التعليمية والقانونية فقط.

---

## 👨‍💻 المطور

**صنع بواسطة:** ! La La la  
**Discord:** @uuuc  
**GitHub:** [@iqnvo](https://github.com/iqnvo)

---

## 📚 مصادر مفيدة

- [pystyle Documentation](https://github.com/billythegoat356/pystyle)
- [pyfiglet Fonts](http://www.figlet.org/examples.html)
- [colorama Guide](https://pypi.org/project/colorama/)
- [Python Terminal Colors](https://www.lihaoyi.com/post/BuildyourownCommandLinewithANSIescapecodes.html)

---

<div align="center">

**مشروع تعليمي من الماضي - قالب للمستقبل** 🚀

Made with ❤️ for learning

</div>

---

**النسخة:** 1.0  
**الحالة:** قالب جاهز ✅  
**الغرض:** تعليمي
